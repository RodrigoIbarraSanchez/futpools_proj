<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLIconFile</key>
			<string></string>
			<key>CFBundleURLName</key>
			<string>com.futpools.invite</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>futpools</string>
			</array>
		</dict>
	</array>
	<key>UIAppFonts</key>
	<array>
		<string>Oxanium.ttf</string>
	</array>
	<!-- Declare supported locales. Without this, iOS treats the app as
	     English-only and never looks up keys in es.lproj — even though the
	     compiled strings file is sitting right there in the bundle. -->
	<key>CFBundleLocalizations</key>
	<array>
		<string>en</string>
		<string>es</string>
	</array>
	<!-- Backend base URL. Edit this string when you switch networks (dev
	     LAN IP), point at staging, or build for prod. APIClient reads it
	     at launch via FPApiBaseURL with a localhost:3000 fallback. -->
	<key>FPApiBaseURL</key>
	<string>http://192.168.100.5:3000</string>
	<!-- Allow plain HTTP to the dev server on the local LAN. App Store
	     builds should point FPApiBaseURL at https://… so this exception
	     becomes inert; remove it before shipping if you want stricter ATS. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSExceptionDomains</key>
		<dict>
			<key>192.168.100.5</key>
			<dict>
				<key>NSExceptionAllowsInsecureHTTPLoads</key>
				<true/>
				<key>NSIncludesSubdomains</key>
				<false/>
			</dict>
		</dict>
	</dict>
</dict>
</plist>
